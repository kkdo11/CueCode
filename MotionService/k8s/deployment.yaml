apiVersion: apps/v1
kind: Deployment
metadata:
  name: motion-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: motion-service
  template:
    metadata:
      labels:
        app: motion-service
    spec:
      containers:
      - name: motion-service-app
        image: cuecode.kr.ncr.ntruss.com/cuecode:motion-service-latest
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: prod,json
        - name: JWT_SECRET # motion-prod.yml의 ${JWT_SECRET}에 주입될 값
          valueFrom:
            secretKeyRef:
              name: jwt-secret      # 시크릿 이름
              key: JWT_SECRET_KEY   # 시크릿 안의 키 이름
        envFrom:
        - secretRef:
            name: motion-db-secret
        ports:
        - containerPort: 15000
        resources:
          requests:
            cpu: "300m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 15000
          initialDelaySeconds: 30 # 시작 시 시간이 더 걸릴 수 있으므로 여유있게 설정
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 15000
          initialDelaySeconds: 60
          periodSeconds: 15
